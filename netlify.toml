[build]
  command = "npm install --legacy-peer-deps && npm run build:prod"
  publish = "dist/andresjosehr-portfolio/browser"
  base = "."

[build.environment]
  NODE_VERSION = "20.19.0"

[functions]
  directory = "netlify/functions"
